# FastAPI Proof of Concept Dependencies

# Core FastAPI
fastapi>=0.104.0
hypercorn>=0.17.3
python-multipart>=0.0.6

# For async operations
asyncio-mqtt>=0.11.0

# Type hints and validation
pydantic>=2.11.3
typing-extensions>=4.8.0

# WebSocket support (handled by hypercorn, no direct dependency needed)

# Database support
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter
alembic>=1.12.0  # Database migrations

# For integration with existing code
# (These should already be in your main requirements.txt)
# celery[redis]
# redis
